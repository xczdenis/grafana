apiVersion: 1

providers:
- name: 'My Dashboards'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  updateIntervalSeconds: 10 # Как часто Grafana будет проверять наличие изменений в файлах дашбордов
  options:
    path: /etc/grafana/provisioning/dashboards
